class Cards {
  String type;
  String number;
  String exp;
  String cvv;
  String holder_name;
  bool isShown;

  Cards(this.type, this.number, this.exp, this.cvv, this.holder_name,
      this.isShown);
}
